class_name: ApiTester
modules:
    enabled: [PhpBrowser, REST,  Db, Asserts, ApiHelper]
    config:
        Db:
            dsn: 'pgsql:host=localhost;dbname=IfigenijaTest'
            user: 'postgres'
            password: 'cokolada543'
            dump: 'tests/_data/DumpFunctional.sql'
            populate: true
            cleanup: false
        PhpBrowser:
            url: http://ifigenija.local:8080
        REST:
            url: http://ifigenija.local:8080
env:
    xdbg: 
        modules:
            config:
                REST:
                    xdebug_remote: netbeans-xdebug
